""" GIPUZKOA
"""

import codecs

class Parser():

    def __init__(self, fin):
        self.file_in = fin
        self.file_out = fout

    def parse(self):
        with open(self.file) as fin:
            with open
            for linea in f:
                if "incidenciaGeolocalizada" in linea:
                    print (linea)


p = Parser("inc2006.xml", "inc2006.csv")
p.parse()
